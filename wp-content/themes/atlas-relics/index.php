<?php
/**
 * Fallback template.
 *
 * Block themes are required to ship an index.php even though templates/index.html
 * is what WordPress actually renders. This file exists only to satisfy that
 * requirement and to fail safely if a non-block-theme context loads it directly.
 *
 * @package Atlas_Relics
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
